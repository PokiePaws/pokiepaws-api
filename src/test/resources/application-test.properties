spring.datasource.url=jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:pokiepaws}
spring.datasource.username=${POSTGRES_USER:pokiepaws}
spring.datasource.password=${POSTGRES_PASSWORD:pokiepaws0303}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

app.jwt.secret=${JWT_SECRET:41008870ef465954c6efa46898366c177e7bfe46475376a0459122bd9c63ef51b2a69538fcda053cd6f02cec5ad02dba009f031b39546ba0d0f2b1065288ae9f}
app.jwt.expiration=${JWT_EXPIRATION:86400000}

app.admin.email=${ADMIN_EMAIL:admin@pokiepaws.pl}
app.admin.password=${ADMIN_PASSWORD:Admin1234!}
app.admin.first-name=${ADMIN_FIRST_NAME:Admin}
app.admin.last-name=${ADMIN_LAST_NAME:PokiePaws}

spring.mail.host=${MAIL_HOST:localhost}
spring.mail.port=${MAIL_PORT:1025}
spring.mail.username=${MAIL_USERNAME:test}
spring.mail.password=${MAIL_PASSWORD:test}
spring.mail.properties.mail.smtp.auth=false
spring.mail.properties.mail.smtp.starttls.enable=false

server.port=${SERVER_PORT:9090}
app.base-url=${BASE_URL:http://localhost:9090}
app.frontend-url=${FRONTEND_URL:http://localhost:3000}